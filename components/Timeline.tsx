import Section from './Section'; import schedule from '@/content/schedule.json'; type Slot={time:string;title:string;note?:string}; export default function Timeline({title='Cronograma do dia'}:{title?:string}){ return(<Section title={title}><ol className='card p-3 sm:p-4 divide-y divide-zinc-800'>{(schedule as Slot[]).map((s,i)=>(<li key={i} className='py-2 sm:py-3 flex items-start gap-3 sm:gap-4'><span className='badge'>{s.time}</span><div><div className='font-medium'>{s.title}</div>{s.note&&<div className='text-zinc-400 text-sm'>{s.note}</div>}</div></li>))}</ol></Section>); }
